# 开发日志 — 2026-05-21

## 今日完成
- [x] 第一阶段全部 6 个子任务：减少代码冗余（安全重构，不改功能）

---

## 1.1 提取公共 KaTeX 渲染函数（utils.js）

- `utils.js` 新增 `safeRenderKatex(latex, target, displayMode)` — 安全渲染 LaTeX 到 DOM 元素
- `utils.js` 新增 `safeRenderKatexToString(latex, displayMode)` — 安全渲染 LaTeX 为 HTML 字符串，失败返回 null
- 替换 `concepts.js` 3 处重复：局部 `safeRender()` 删除、`renderConceptFormulas` 内联 try/catch、`renderMixedLatex` 的 3 处 `katex.renderToString()`
- 替换 `latexRef.js` 1 处：`renderLatexRefView` 内联 try/catch
- 替换 `mindmap.js` 1 处：`renderMindmap` 节点公式渲染内联 try/catch
- 附带修复：错误提示色 `var(--red-500)`（CSS 中不存在）→ `var(--red)`

## 1.2 消除例题筛选重复（examples.js）

- 新增 `filterExamples(allExamples, query, courseId)` 纯函数
- `renderExampleView()` 和 `refreshExampleResults()` 的相同 15 行过滤逻辑合并为 1 行调用

## 1.3 提取嵌套数据操作辅助（storage.js）

- `saveCourses` / `saveExamples` 增加 `QuotaExceededError` 防护（存储爆仓异常）
- 新增 4 个嵌套数据辅助函数：
  - `getChapter(courseId, chapterId)` → `{ course, chapter }` 或 null
  - `getConcept(courseId, chapterId, conceptId)` → `{ course, chapter, concept }` 或 null
  - `saveConcept(courseId, chapterId, concept)` → upsert + 自动写回
  - `deleteConcept(courseId, chapterId, conceptId)` → 删除 + 自动写回
- `concepts.js` 3 处嵌套查找替换为辅助函数调用（保存/编辑/删除概念）

## 1.4 消除 setTimeout(50) 硬编码延迟（app.js + 4 个调用方）

- `app.js` `showModal` 新增第 4 个参数 `onReady(overlay)`，DOM 插入后同步回调
- 消除 `courses.js` `showNewCourseModal` 颜色选择器 setTimeout
- 消除 `examples.js` `showExampleDetailModal` PDF 链接 setTimeout
- 消除 `examples.js` `showExampleModal` Tag/文件上传 setTimeout
- 消除 `mindmap.js` `showColorPicker` 颜色点击 setTimeout
- 附带：DOM 查询从 `document.querySelectorAll` 改为 `overlay.querySelectorAll`（作用域隔离）

## 1.5 提取高频 CSS 工具类（styles.css + 6 个 JS 文件）

- `styles.css` 新增 12 个工具类：`.flex-between`、`.flex-center`、`.flex-wrap`、`.flex-col`、`.gap-xs/sm/md/lg`、`.text-muted`、`.mb-xs/sm/md`
- 替换 6 个 JS 文件中约 23 处内联 `style="..."` 为对应 class

## 1.6 清理冗余排查

- `el()` 函数：仅被 `showToast` 使用，保留（showToast 是全项目最频繁调用的工具函数）
- `escapeHtml` 漏用排查：54 处用户输入插值全部正确转义，无漏用

---

## 第一阶段收尾统计

| 子任务 | 文件数 | 净减少行数 |
|--------|:--:|:--:|
| 1.1 KaTeX 渲染统一 | 5 | ~25 |
| 1.2 例题筛选合并 | 1 | ~15 |
| 1.3 嵌套数据操作 | 2 | ~20 |
| 1.4 消除硬编码延迟 | 5 | ~15 |
| 1.5 CSS 工具类 | 8 | ~0 |
| 1.6 清理排查 | 0 | 0 |
| **合计** | **8** | **~75** |

---

## UI 重构：美式复古杂志/报纸风

### 配色体系
- 新增三辅色：豆沙色(`--rose*`)、灰蓝(`--slate*`)、鼠尾草绿(`--sage*`)
- 纸张色系：`--paper` / `--paper-card` / `--paper-white` / `--paper-dark`
- 墨色系：`--ink` / `--ink-light` / `--ink-muted`
- 全局对比度大幅提升（暖调炭黑 #2c241c 替代暖灰色 #3d3835）

### 视觉效果
- 硬阴影替代模糊阴影（`4px 4px 0 var(--ink)`）
- 圆角缩至 2-4px（原 10-14px）
- 1-2px 纯黑实线边框替代虚线/双线分割线
- 大标题引入 Playfair Display 衬线体
- Lucide 线条图标全量替换 emoji
- 弹窗 title 使用衬线体 + 实线分割线

### 修改文件
- `src/css/styles.css` — 全局重写（19 个 CSS 变量全部替换 + 全部组件样式重写）
- `src/index.html` — 引入 Google Fonts + Lucide CDN，页面内 emoji → Lucide 图标
- `src/js/app.js` — fallback emoji + showModal 添加 lucide.createIcons()
- `src/js/courses.js` — emoji + CSS 变量替换 + lucide.createIcons()
- `src/js/chapters.js` — emoji + CSS 变量替换 + lucide.createIcons()
- `src/js/concepts.js` — emoji + CSS 变量替换 + lucide.createIcons()
- `src/js/examples.js` — emoji + CSS 变量替换 + lucide.createIcons()
- `src/js/latexRef.js` — emoji + CSS 变量替换 + lucide.createIcons()
- `src/js/mindmap.js` — emoji + CSS 变量替换 + lucide.createIcons()
